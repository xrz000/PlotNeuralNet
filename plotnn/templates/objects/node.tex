\pic[shift={ {{- offset -}} }] at {{ location }} {
    Ball={
        name={{ name }},
        fill={{ color|default("\SumColor") }},
        opacity={{ opacity }},
        radius={{ radius }},
        logo=${{ logo }}$
    }
};
